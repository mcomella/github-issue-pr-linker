/*
 * A wrapper around GithubEndpoint that provides caching.
 */
namespace Github {

    export function getOpenPRs(owner: string, repo: string): Array<GithubEndpoint.PR> {
        return [];
    }
}
