{
    "compilerOptions": {
        "outFile": "./dist/tests.spec.js",
        "sourceMap": true,

        "strictNullChecks": true,
        "noImplicitAny": true,
        "noImplicitThis": true,

        "target": "es6",
    },

    // TODO: consider inheriting the files here.
    "include": [
        "../typings",
        "../src/ts", /* Ideally, we'd use the compiled output but we'd lose type info. */
        "../src/options/options_store.ts",
        "./",
    ]
}
